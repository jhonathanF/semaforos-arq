export class PeopleTrafficLight {
    status: number;
    assetUrl: string;

    constructor(status: number, assetUrl: string) {
        this.status = status;
        this.assetUrl = assetUrl;
    }
}
